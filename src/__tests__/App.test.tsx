import { describe, expect, test } from 'vitest'

import App from '../App'

describe('Accordion test', () => {
  test('app child components should be defined', () => {
    expect(App).toBeDefined()
  })
})
