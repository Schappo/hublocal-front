import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    success: {
      main: '#00C853',
      dark: '#007E33',
      contrastText: '#FFFFFF',
    },
    primary: {
      main: '#0485FF',
      dark: '#0057B2',
      contrastText: '#FFFFFF',
    },
  },
})
